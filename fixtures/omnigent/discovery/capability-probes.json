{
  "schema": "omnigent.capability_probe_matrix.v1",
  "redaction": "metadata_only",
  "captured_at": "2026-08-12T19:00:00Z",
  "capabilities": [
    { "name": "create_session", "status": "supported", "evidence": ["v0.9 POST /v1/sessions requires agent_id and initial_items", "named-agent JSON create is supported", "idempotency is process-local", "bundle and inline specs remain blocked"] },
    { "name": "send_turn", "status": "supported", "evidence": ["v0.9 tagged message role/content event", "accepted and synchronous policy-denial acknowledgements"] },
    { "name": "stream_events", "status": "supported", "evidence": ["v0.9 GET /stream", "52-event allowlist", "tagged SSE normalization"] },
    { "name": "read_history", "status": "supported", "evidence": ["cursor-paginated ConversationItem history"] },
    { "name": "cancel_turn", "status": "supported", "evidence": ["interrupt event", "requires withExclusiveSessionLease because the control is session-scoped"] },
    { "name": "close_session", "status": "emulated", "evidence": ["stop_session plus provider-local logical close", "requires withExclusiveSessionLease because the control is session-scoped"] },
    { "name": "list_sessions", "status": "supported", "evidence": ["cursor-paginated GET /v1/sessions"] },
    { "name": "harness_catalog", "status": "supported", "evidence": ["read-only GET /v1/harnesses"] },
    { "name": "child_session", "status": "blocked", "evidence": ["child endpoint is observe-only", "routed_model and routing_decision_id are read-only", "smart routing grants no lease or lock authority"] },
    { "name": "harness_override", "status": "blocked", "evidence": ["catalog is read-only", "routing override is not public harness authority"] },
    { "name": "malformed_event", "status": "emulated", "evidence": ["unknown SSE frames are skipped", "invalid event requests fail"] },
    { "name": "reconnect", "status": "supported", "evidence": ["stream handshake precedes snapshot and paginated history"] },
    { "name": "terminal_event_uniqueness", "status": "emulated", "evidence": ["provider collapses response and turn terminal markers"] }
  ],
  "provenance": [
    { "kind": "tagged_source", "path": "omnigent/server/API.md", "ref": "v0.9.0" },
    { "kind": "tagged_source", "path": "openapi.json", "ref": "v0.9.0" },
    { "kind": "tagged_source", "path": "omnigent/server/schemas.py", "ref": "v0.9.0" }
  ]
}
