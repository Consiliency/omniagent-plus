{
  "schema": "omnigent.capability_probe_matrix.v1",
  "redaction": "metadata_only",
  "captured_at": "2026-08-24T06:00:29Z",
  "release": {
    "tag": "v0.10.0",
    "commit": "40755dd8dddb07e1eb6e4055d1d9936e184ceb9b"
  },
  "capabilities": [
    { "name": "create_session", "status": "supported", "evidence": ["v0.10 POST /v1/sessions requires agent_id and initial_items", "named-agent JSON create is supported", "idempotency is process-local", "bundle and inline specs remain blocked"] },
    { "name": "send_turn", "status": "supported", "evidence": ["v0.10 tagged message role/content event", "accepted and synchronous policy-denial acknowledgements"] },
    { "name": "stream_events", "status": "supported", "evidence": ["v0.10 GET /stream", "52-event allowlist", "tagged SSE normalization", "v0.11 session.permission_mode and session.title are excluded"] },
    { "name": "read_history", "status": "supported", "evidence": ["cursor-paginated ConversationItem history"] },
    { "name": "cancel_turn", "status": "supported", "evidence": ["interrupt event", "requires withExclusiveSessionLease because the control is session-scoped"] },
    { "name": "close_session", "status": "emulated", "evidence": ["stop_session plus provider-local logical close", "requires withExclusiveSessionLease because the control is session-scoped"] },
    { "name": "list_sessions", "status": "supported", "evidence": ["cursor-paginated GET /v1/sessions"] },
    { "name": "harness_catalog", "status": "supported", "evidence": ["read-only GET /v1/harnesses"] },
    { "name": "child_session", "status": "blocked", "evidence": ["child endpoint is observe-only", "routed_model, routing_decision_id, and nullable task_summary are read-only", "smart routing grants no child-create, route-decision, lease, or lock authority"] },
    { "name": "harness_override", "status": "blocked", "evidence": ["catalog is read-only", "routing override is not public harness authority"] },
    { "name": "malformed_event", "status": "emulated", "evidence": ["unknown SSE frames are skipped", "invalid event requests fail"] },
    { "name": "reconnect", "status": "supported", "evidence": ["stream handshake precedes snapshot and paginated history", "identity-free equal text remains lossless"] },
    { "name": "terminal_event_uniqueness", "status": "emulated", "evidence": ["provider collapses response and turn terminal markers"] }
  ],
  "observed_non_capabilities": [
    { "name": "bundle_root_isolation", "evidence": "stable v0.10 upstream runtime guarantee", "provider_capability": false, "transport_enforced": false },
    { "name": "structured_error_descriptions", "evidence": "lossless title, cause, remediation, and unknown fields", "provider_capability": false, "classification_authority": false },
    { "name": "shared_editor_approval", "evidence": "v0.10 upstream rollback permits shared editors to approve", "provider_capability": false, "consiliency_authority": false },
    { "name": "usage_and_admin_surfaces", "evidence": "usage, branding, server discovery, smart-routing sources, and environment search", "provider_capability": false, "consiliency_authority": false }
  ],
  "provenance": [
    { "kind": "tagged_source", "path": "omnigent/server/API.md", "ref": "v0.10.0" },
    { "kind": "tagged_source", "path": "openapi.json", "ref": "v0.10.0" },
    { "kind": "tagged_source", "path": "omnigent/server/schemas.py", "ref": "v0.10.0" },
    { "kind": "tagged_source", "path": "CHANGELOG.md", "ref": "v0.10.0" },
    { "kind": "tagged_source", "path": "tests/runner/test_sub_agent_workdir_resolution.py", "ref": "v0.10.0" }
  ]
}
